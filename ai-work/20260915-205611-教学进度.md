# 教学进度报告

- 任务完成详细时间：2026-09-15 20:56:11
- 当前工作的模型名：deepseek/deepseek-v4-flash（完整 ID：pq/deepseek/deepseek-v4-flash）
- 依据文档：`docs/learning-roadmap.md`、`AGENTS.md`

---

## 一、总体进度（阶段一：C++ 类型系统与语言深度，第 1~10 周）

| 周次 | 主题 | 优先级 | 状态 |
|---|---|---|---|
| 第 1 周 | const 正确性 | P0 | 已完成 |
| 第 2 周 | 类型转换与 explicit | P0 | 已完成 |
| 第 3 周 | 运算符重载 | P1 | 已完成 |
| 第 4 周 | 智能指针 | P0 | 进行中（2/6 模块） |
| 第 5~10 周 | 移动语义 / Lambda / STL / 编译链接 / 虚函数 / 异常安全 | - | 未开始 |

依据：第 1 周笔记见仓库根 `Readme.md`；第 2、3 周有对应 git 提交（`类型转换和explicit`、`1.运算符重载上半部分`、`第三周最后的模块`）。

---

## 二、第 3 周明细（已完成，6 个模块）

1. 哪些运算符可/不可重载（`::` `.` `.*` `?:` 不可；`&&` `||` `,` 能但不该）
2. 算术运算符：成员函数 vs 非成员函数（对称性、左操作数转换）
3. 比较/赋值运算符 + copy-and-swap idiom
4. `operator[]` 的 const 重载
5. 类型转换运算符 `explicit operator bool()`
6. 不应重载的 `&&` `||` `,`（短路/求值顺序丢失）

验证事实：copy-and-swap 三性质（拷贝赋值正确性、自赋值安全、强异常安全）已在 `03_week/main.cpp` 的 `test4()` 用 `assert` 验证通过；异常注入手法为静态开关 `String::throwOnCopy`。

---

## 三、第 4 周明细（进行中）

| 模块 | 主题 | 状态 |
|---|---|---|
| 1 | unique_ptr 基础 | 已讲 + 自测 2 题（delete/delete[] 陷阱、make_unique 异常安全） |
| 2 | unique_ptr 自定义 deleter | 已讲，答疑中 |
| 3 | shared_ptr | 未开始 |
| 4 | weak_ptr | 未开始 |
| 5 | enable_shared_from_this + aliasing ctor | 未开始 |
| 6 | PIMPL + Qt 混用（QPointer/QSharedPointer） | 未开始 |

- 模块 1 覆盖：独占所有权、不可拷贝可移动、`make_unique`、`delete`/`delete[]` 配对、EBO 后 size 等于裸指针。
- 模块 2 覆盖：自定义 deleter（functor / lambda / 函数指针）、deleter 是类型的一部分（对比 shared_ptr 类型擦除）、size 影响（8 / 16 / 8+捕获）、`make_unique` 不支持自定义 deleter；用户追问 extended 到函数指针本质、function-to-pointer decay、`decltype` 用途、函数指针大小。

---

## 四、关键决策与权衡

1. 补缺按 P0→P1→P2，不按原周次顺序（依据摸底诊断结论：阶段一、二约 80% 未掌握）。
2. 教学推进规则两次强化并写入 `AGENTS.md` §2（均由用户打回后固化）：
   - 「自测题」改为独立推进单位：必须先等用户明确表示理解当前讲解，才允许出自测题。
     - 权衡：牺牲推进速度，换取「讲一段、懂一段」，杜绝「没懂就被推着走」。
   - 无用户明确授权禁止创建/修改/删除任何源码文件。
     - 权衡：AI 曾把「怎么验证」误判为写文件授权，擅自创建 `03_week/copy_and_swap_test.cpp` 并编译运行；此后验证代码一律只放对话，仅当用户明确说「写入/保存到 .cpp」才落盘。
3. 测试方式：用 `<cassert>` 手写断言，不引入 GoogleTest（路线图第 54 周才讲，避免提前引入认知负担）。
   - 代价：无测试框架统一管理，仅适合当前单文件阶段。

---

## 五、环境与事实记录

- 编译器：g++ 15.1.0（MinGW，x86_64-posix-seh）；默认标准 C++17（已运行 `__cplusplus` 宏查证 = `201703L`）。
- 构建：CMake 3.10+ / MinGW Makefiles；输出到 `bin/`。
- 已知风险：`CMakeLists.txt` 未显式 `set(CMAKE_CXX_STANDARD ...)`，仅依赖编译器默认标准，换环境可能不一致。
- 仓库：本地 `master` 分支，工作区干净；远程 `git@github.com:mriaclewonder/Learn.git` 存在且为空，本地尚未关联 remote。

---

## 六、下一步

1. 继续第 4 周模块 3：shared_ptr（引用计数、`make_shared` 单次分配优化、控制块）。
2. 待办（独立于教学）：显式设置 `CMAKE_CXX_STANDARD`；执行 `git remote add origin` 并首次 `push`。
